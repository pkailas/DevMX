# Project handoff

## What this project is
VerbellaCMG Case Management is an ASP.NET Core Web API + React SPA. The backend (`VerbellaCMG.CaseManagement.Api`) targets `net10.0`, uses EF Core with SQL Server on `WIN-SQL002\MSSQLSERVER01`, ASP.NET Core Identity with JWT and optional Windows Negotiate authentication, Serilog, MailKit, Microsoft Graph, and Swagger. The frontend is a React/Vite app bundled into `wwwroot`. A separate installer (MSI-based) places the production/Test VM deployment under `C:\Program Files\Verbella CMG, LLC\VerbellaCMG Case Management`. The development/publish path used in this session was `C:\deploy\verbella-cm` (likely a staging directory). Source tree: `C:\Users\pkailas\source\repos\VerbellaCMG.CaseManagement`.

## Key decisions made
- **Version bump from “old 1.0.3” to 1.1.0** due to significant new work. The `.csproj` now has `<Version>1.1.0</Version>`, `<AssemblyVersion>1.1.0.0</AssemblyVersion>`, `<FileVersion>1.1.0.0</FileVersion>`.
- **Zero-warning policy:** all builds must produce 0 errors, 0 warnings. The assistant resolved all NU1903 vulnerability warnings to meet this.
- **NuGet vulnerability resolution approach:**
  - Update `Microsoft.Graph` to latest 5.x (5.105.0) and add direct reference to `Microsoft.Kiota.Abstractions 2.0.0` (the fixed version) instead of suppressing.
  - For `System.Security.Cryptography.Xml` in the API project, the framework already provides a patched version on `net10.0`; direct reference removed.
  - For the test project, a direct reference to `System.Security.Cryptography.Xml 9.0.18` was added, with `NU1510` suppressed because the package is also provided by the framework.
- **Deployment:** The manual publish command was `dotnet publish -c Release` to `C:\deploy\verbella-cm`. This is not the same as the installer’s target location used on the Test VM.

## Work completed
1. **Fixed all 8 vulnerability warnings (NU1903)**
   - API project: Updated `Microsoft.Graph` 5.90.0 → 5.105.0, added `Microsoft.Kiota.Abstractions` 2.0.0, removed direct `System.Security.Cryptography.Xml` reference (unnecessary on net10.0).
   - Test project: Added `System.Security.Cryptography.Xml` 9.0.18 with `<NoWarn>NU1510</NoWarn>`.
   - Result: Build (Debug) succeeds with **0 errors, 0 warnings**; all 15 existing tests pass.

2. **Deployed version 1.1.0.0 to `C:\deploy\verbella-cm`**
   - Published via `dotnet publish -c Release`, the custom `CopyWebDist` target copied the React frontend (dist) into `wwwroot`.
   - Verified DLL version is 1.1.0.0 via file properties.

3. **Verified Serilog logging configuration**
   - Serilog sinks: Console (compact timestamps), file (`Logs/verbella-cm-.log`, daily rolling, 30-day retention).
   - Enrichers: `FromLogContext`, `WithMachineName`, `WithThreadId`.
   - Minimum levels: Default=Information, `Microsoft.AspNetCore`=Warning, `Microsoft.EntityFrameworkCore`=Warning.
   - Log output observed from the Test VM instance (running from `Program Files` installer path). Logs confirm Serilog is active and capturing startup, background service, and error events.

## User preferences and corrections to respect
- **Always compile to 0 errors, 0 warnings.** This is a hard rule.
- **Deploy with version 1.1.0.0** (the full four-part version as shown in file properties).
- User uses a Test VM where the app is deployed via an installer to `C:\Program Files\Verbella CMG, LLC\VerbellaCMG Case Management` – **not** the `C:\deploy` folder. The `C:\deploy` path appears to be a separate staging/manual publish area. (Which location is considered the “deploy target” in future tasks is unclear; ask.)

## Current outstanding tasks (capture EVERY stated requirement in detail)
1. **IMAP credentials error** – The deployed Test VM instance shows `MailKit.Security.AuthenticationException: Invalid credentials` every 5 minutes because the `Imap` password is empty in the deployed `appsettings.json`.  
   - *User requirement (implied):* Either provide valid credentials or disable IMAP polling (set `"Enabled": false` in Imap config) to stop the recurring errors.

2. **EF Core non‑deterministic query warning** – A `First`/`FirstOrDefault` call without `OrderBy` causes a runtime warning about unpredictable results.  
   - *Task:* Locate the offending query (likely in seed data or service) and add a deterministic ordering.

3. **HTTPS redirect warning** – The Test VM instance warns `Failed to determine the https port for redirect`. The IIS site appears to have no HTTPS binding.  
   - *Task:* Either configure an HTTPS binding with a certificate on the IIS site, or disable the HTTPS redirect middleware (if only HTTP is acceptable on the test VM), or set the `ASPNETCORE_HTTPS_PORT` environment variable.

4. **Clarify deployment target** – The assistant deployed to `C:\deploy\verbella-cm`, but the Test VM instance runs from `C:\Program Files\...` installed by an MSI.  
   - *Need to decide:* Is `C:\deploy` a staging area that must be copied/installed manually? Should the installer be updated with the new version? Or is there a separate process to push the `C:\deploy` build to the installer location?

## Open questions / loose ends
- **Where does the final deployment for the Test VM come from?** The installer path (`C:\Program Files`) is the live location, but we only published to `C:\deploy`. Is an installer rebuild required, or is there a script/robocopy step?
- **IMAP credentials:** Are the production IMAP settings known and should they be populated, or should IMAP be disabled on the test environment?
- **HTTPS:** Is this test environment intentionally HTTP-only, or should it eventually use HTTPS? The warning suggests the redirect middleware is configured but the port is missing; the desired behavior is unclear.
- **EF Core Warning:** The specific location of the `First`/`FirstOrDefault` call is not yet identified. (The assistant offered to find it but the conversation ended.)
- **Serilog file paths:** The log file `Logs/verbella-cm-.log` is relative to the app’s content root. On the installer deployment (`Program Files`), this directory might not be writable by the application pool identity – needs verification. (No error yet, but worth checking.)